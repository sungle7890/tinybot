# 05. 미결정 사항

> English: [05-open-questions_eng.md](05-open-questions_eng.md)

Phase 1로 넘어가기 전에 정해야 하는 것들. 정해지면 이 문서에 결정과 근거를 남긴다.

## Q1. 보드 — 어디까지 갈 것인가 ★ 가장 중요

| 안 | 내용 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **A. MCU 단독** (ESP32-S3) | 아두이노 프로젝트로 끝까지 | 저비용, 전력 이해도, 실시간 제어 감각 | 비전 불가, 5단계 어려움 |
| **B. MCU + 호스트 PC** | MCU는 제어, 학습은 노트북 | A의 장점 + 무거운 학습 가능 | 무선 의존, 로봇이 PC에 묶임 |
| **C. MCU + Pi Zero 2 W** | 호스트를 로봇에 탑재 | 완전 자율 + 비전 | 비용·무게·전력 증가 |
| **D. Microduck 구매** | $399 | 즉시 최신 RL 로봇 | 만드는 재미가 없음. 배움이 얕아짐 |

**결정: A (MCU 단독).** 2026-09-04.
**보드 변경: Arduino UNO R4 WiFi.** 2026-09-05 — 이미 보유 중이라 $15 절약,
그리고 온보드 ESP32-S3 모듈로 무선 텔레메트리가 열린다. ESP32-S3 DevKitC는
폐기가 아니라 **Phase 4용으로 보류**한다. 펌웨어는 `src/hal/` 뒤에서 두 보드를
모두 지원하므로 나중에 바꿔도 재작성이 없다.
B·C는 폐기가 아니라 보류다. A→B→C는 같은 하드웨어 위에서 순차 확장이 가능하도록
[02-architecture.md](02-architecture.md)의 2계층 구조를 잡아뒀다.
A로 시작해도 나중에 C로 가는 데 낭비가 없다.

D는 "만들기"가 아니라 "쓰기"이므로 이 프로젝트의 목표와 다르다.
다만 오픈소스이므로 **구매하지 않고 코드만 읽는 것**은 강력히 권장한다.

## Q2. 비전(카메라)이 필요한가

- 필요 없다면 → ESP32-S3 또는 Nano 33 BLE Sense, 어느 쪽이든 무방
- 필요하다면 → ESP32-S3(저해상도 한정) 또는 Pi 계열이 사실상 강제

**결정: 지금은 안 쓴다.** 2026-09-04.
3단계 Q-learning에는 카메라가 필요 없다. ESP32-S3를 골랐으므로
나중에 저해상도 카메라를 붙이는 선택지는 열려 있다.

## Q3. 로코모션 — 바퀴인가 다리인가

Microduck은 서보 15개 이족이다. 매력적이지만:

- 이족 보행은 실기 시행착오로 학습시킬 수 없다 (로봇이 부서진다)
- 반드시 시뮬레이터가 먼저 필요하다 → 5단계 선행
- 학습 자체보다 기구·제어에 시간의 대부분이 들어간다

**결정: 2WD 바퀴.** 2026-09-04.
 "학습이 일어나는가"를 검증하는 데 집중할 수 있다.
다리는 3단계를 끝낸 뒤 별도 프로젝트로.

## Q4. 펌웨어 언어

| 안 | 판단 |
|---|---|
| **C++ / Arduino (PlatformIO)** | 자료·라이브러리 압도적. 기본값 |
| Rust (esp-hal) | Microduck과 같은 선택. 안전하지만 임베디드 생태계 학습 비용이 크다 |
| MicroPython | 프로토타이핑은 빠르나 50 Hz 결정론적 루프에 부적합 |

**결정: PlatformIO + C++.** 2026-09-04.
첫 임베디드 프로젝트에서 언어까지 새로 배우면
디버깅 시 "내 로직 문제인가 언어 문제인가"를 구분하기 어렵다.

## Q5. 예산 상한

A안 기준 약 10~12만원. Pi Zero 2 W는 보류했으므로 추가 비용 없음.

**결정: 부품 ~$202** (공구 별도). 2026-09-05.

---

## 결정 기록

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-04 | 레포 생성, 2계층 아키텍처 채택 | 어떤 보드를 고르든 낭비되지 않는 구조 |
| 2026-09-04 | **Q1 = A: ESP32-S3 단독** | 듀얼코어로 제어 루프와 통신을 물리적으로 분리 가능. B·C로 확장할 때 A의 펌웨어가 그대로 남는다 |
| 2026-09-04 | **Q2 = 카메라 보류** | 3단계까지 불필요. ESP32-S3라 나중에 붙일 여지는 남는다 |
| 2026-09-04 | **Q3 = 2WD 바퀴** | 이족은 실기 시행착오 학습이 불가능해 시뮬레이터가 선행되어야 한다 |
| 2026-09-04 | **Q4 = PlatformIO + C++** | 첫 임베디드에서 언어까지 새로 배우면 디버깅 시 원인 분리가 안 된다 |
| 2026-09-04 | Arduino core 2.x(espressif32 공식 플랫폼) 사용 | 검증된 조합. LEDC 호출은 버전 가드를 넣어 core 3.x에서도 빌드된다 |
| **2026-09-05** | **주 보드를 UNO R4 WiFi로 변경, ESP32-S3는 Phase 4용 보류** | 이미 보유. Phase 1~3에는 충분하고 $15를 아낀다. HAL을 도입해 두 보드를 동시에 지원하므로 되돌리는 비용이 없다 |
| 2026-09-05 | 범퍼를 인터럽트에서 **폴링**으로 변경 | R4 WiFi는 외부 인터럽트 핀이 D2/D3 둘뿐이고 엔코더가 그 둘을 써야 한다. 벽에 눌린 스위치는 20 ms 틱보다 훨씬 오래 닫혀 있어 놓치지 않는다 |
| 2026-09-05 | I2C를 **Qwiic(Wire1, 3.3 V)**으로 | 헤더 핀을 쓰지 않아 A4/A5가 비고, 그래야 핀 맵이 들어간다. Adafruit 센서는 STEMMA QT라 납땜도 없다 |
| 2026-09-05 | R4 제어 루프는 **협조적 스케줄링** (`micros()` 기준) | FspTimer API 리스크를 피했다. 실제 지터는 `j` 명령이 판정한다 — 예산을 못 지키면 `hal_r4.cpp` 안에서만 고치면 된다 |
| 2026-09-05 | 전원 보드를 **#3543 → #3541 + TB6612**로 | R4가 VIN 6~24 V를 자체 레귤레이션하므로 #3543의 2 A 레귤레이터가 불필요해졌다. #3541은 전원만 담당해 TB6612를 유지할 수 있고, 그래서 펌웨어·핀맵을 안 고친다. $15 절약 |
| 2026-09-15 | R4 업로드를 **pyOCD(CMSIS-DAP)**로 | bossac가 x86_64 전용이라 Apple Silicon에서 실행 불가, OpenOCD는 RA4M1 플래시 드라이버가 없다. pyOCD + Renesas 팩으로 0x4000부터 섹터 단위로 써서 부트로더를 보존한다. 출고 데모 스케치는 백업 없이 덮어썼다(사용자 결정) |
